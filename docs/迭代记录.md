# 自主迭代记录

> 2026-09-10 夜间自主迭代会话的 Task 日志（coordinator：ZCode）。编号 Task-xx 仅为本
> 日志条目编号，与 relay-harbor 设计条目（FR-xxx / ADR-xxx / TASK-xxx）无关；条目内容
> 若触及设计编号会显式标注。每个 Task 在独立 worktree（`.worktrees/<名>` + 分支
> `iter/<名>`）迭代，由 coordinator 审阅后 rebase 合并回 master，合并前过全量门禁
> （`pnpm run check` + `cargo test`）。

## Task 总览

| 编号 | 主题 | worktree / 分支 | 状态 |
| --- | --- | --- | --- |
| Task-01 | 聊天流链路测试补强（streamHub / StreamingMessage） | `wave1-chat-tests` / `iter/wave1-chat-tests` | 进行中 |
| Task-02 | 设置与角色编辑器测试补强（表单逻辑 / 纯函数 / 组件冒烟） | `wave1-features-tests` / `iter/wave1-features-tests` | 进行中 |
| Task-03 | Rust 侧审计加固（clippy 清零 / 测试补强 / 边界复核） | `wave1-rust-hardening` / `iter/wave1-rust-hardening` | 进行中 |
| Task-04 | （规划中）角色卡导入/导出 | 待开 | 未开始 |
| Task-05 | （规划中）导演/结算服务切片 | 待开 | 未开始 |

## Task-01 聊天流链路测试补强

- **动机**：`src/features/chat/` 是全应用最核心的链路（多路并发生成、切走不取消、
  终态落库），但 `streamHub.ts`、`StreamingMessage.tsx` 无单测，回归全靠手测。
- **范围**：仅新增 `src/features/chat/*.test.ts(x)`；发现源码 bug 只记录 + 最小修复。
- **验收**：新增测试覆盖 streamHub 状态机全迁移路径与 StreamingMessage 渲染分支；
  `pnpm test` 全绿；不改动 `src/api/` 层契约。

## Task-02 设置与角色编辑器测试补强

- **动机**：`useEditorForm`（整卡覆盖表单 + 模型覆写解析）、`preferences`、
  `posterGradient`、`SettingsCard` 均无或缺测试；编辑器近期连续重构（强调色、
  性别年龄、人设展示态），回归风险高。
- **范围**：仅新增 `src/features/settings/*.test.*`、`src/features/characters/**/*.test.*`。
- **验收**：表单校验、覆写解析、纯函数边界全覆盖；组件冒烟至少覆盖挂载与关键交互；
  `pnpm test` 全绿。

## Task-03 Rust 侧审计加固

- **动机**：`generation.rs`（1100+ 行）承载流式生成全状态机，`infra/llm`（SSE 解析、
  think 抽取）是网络边界；借整夜窗口做一次 clippy 清零 + 边界复核 + 测试补强。
- **范围**：仅 `src-tauri/`；**不改 IPC 命令签名**（避免 bindings 再生成波及前端）、
  不新增 crate 依赖；四层边界（`interfaces → services → domain ← infra`）必须保持。
- **验收**：`cargo test` 全绿、`cargo clippy` 无警告、`node scripts/check-rust-boundaries.mjs`
  通过；改进点逐条记录。

## Task-04 角色卡导入/导出（规划中）

- **动机**：数据全本地铁律下，角色卡的可迁移性（JSON 导出/导入）是自用刚需；
  `Character` 模型字段已稳定（含强调色/性别/年龄/日历配置预留）。
- **形态**：预期跨 Rust（interfaces/services）+ 前端（characters feature + api + i18n）
  的完整纵向切片，新增 IPC 命令需登记白名单。开工前细化。

## Task-05 导演/结算服务切片（规划中）

- **动机**：`services/director.rs` 仍是占位符，而数据地基（`scenes` / `character_states`
  表、`calendar_config` 字段）已随迁移 0002/0003/0005 就位；FR-011/012/013 的
  结算链路是从「纯聊天」迈向「叙事状态追踪」的第一块实体。
- **形态**：保守切片——按代码内注释已表达的设计意图实现，不做激进发挥。开工前细化。
