// @ts-nocheck

// This file was generated by [tauri-specta](https://github.com/oscartbeaumont/tauri-specta). Do not edit this file manually.

/** user-defined commands **/


export const commands = {
async listSessions() : Promise<Result<SessionSummary[], IpcError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("list_sessions") };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async createSession(characterId: number, title: string | null) : Promise<Result<SessionSummary, IpcError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("create_session", { characterId, title }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async deleteSession(sessionId: number) : Promise<Result<null, IpcError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("delete_session", { sessionId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async listMessages(sessionId: number) : Promise<Result<ChatMessage[], IpcError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("list_messages", { sessionId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async sendMessage(sessionId: number, content: string) : Promise<Result<ChatMessage, IpcError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("send_message", { sessionId, content }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async cancelGeneration(sessionId: number) : Promise<Result<boolean, IpcError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("cancel_generation", { sessionId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async regenerateLast(sessionId: number) : Promise<Result<ChatMessage, IpcError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("regenerate_last", { sessionId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async listCharacters() : Promise<Result<CharacterSummary[], IpcError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("list_characters") };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async createCharacter(input: CharacterInput) : Promise<Result<CharacterSummary, IpcError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("create_character", { input }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async updateCharacter(id: number, input: CharacterInput) : Promise<Result<null, IpcError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("update_character", { id, input }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async deleteCharacter(id: number) : Promise<Result<null, IpcError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("delete_character", { id }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async getConfig() : Promise<Result<ConfigDto, IpcError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("get_config") };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async saveConfig(config: ConfigDto) : Promise<Result<null, IpcError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("save_config", { config }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
}
}

/** user-defined events **/


export const events = __makeEvents__<{
streamEvent: StreamEvent
}>({
streamEvent: "stream-event"
})

/** user-defined constants **/



/** user-defined types **/

/**
 * 新建 / 更新角色卡入参（FR-006；整卡覆盖语义见 UpdateCharacter）。
 */
export type CharacterInput = { name: string; 
/**
 * None = 不带头像 / 更新时清除头像。
 */
avatar: string | null; persona: string; greeting: string; renderStyle: string; modelConfig: string | null; 
/**
 * 强调色 #RRGGBB，可空；None = 跟随海报派生色。
 */
accentColor: string | null; 
/**
 * TTS 预留缝（CON-003），前端恒传 null。
 */
voiceConfig: string | null }
/**
 * 角色卡摘要（角色页卡片；session_count 为关系侧汇总）。
 * 
 * TASK-008 起 `persona` / `model_config` 随列表返回：编辑表单点选即载入全量
 * 字段（UI-002），避免为预填再发一次单条查询。
 */
export type CharacterSummary = { id: number; name: string; 
/**
 * 头像可空：data URL 或 `~/.chronoveil` 相对路径；null 时前端首字占位。
 */
avatar: string | null; 
/**
 * 人设系统提示词（编辑预填）。
 */
persona: string; 
/**
 * 出场动画风格（18 种之一，FR-005）。
 */
renderStyle: string; 
/**
 * 开场白 markdown-lite。
 */
greeting: string; 
/**
 * 每角色模型覆写 JSON（camelCase 键，`resolve_effective_llm` 消费）；
 * None = 跟随全局默认。
 */
modelConfig: string | null; 
/**
 * 强调色 #RRGGBB，可空；None = 跟随海报派生色（前端 accentColorOf）。
 */
accentColor: string | null; updatedAt: number; 
/**
 * 该角色开启的会话数（在世会话）。
 */
sessionCount: number }
/**
 * 聊天消息（前端 ChatMessage；characterId 由命令层派生：
 * assistant → 所属会话的角色，user → null——messages 表不冗余存说话人）。
 */
export type ChatMessage = { id: number; sessionId: number; characterId: number | null; role: MessageRole; 
/**
 * 原始 markdown-lite 正文，显示时才解析（BR-005）。
 */
content: string; 
/**
 * 思考内容，与正文分离（FR-003）。
 */
reasoning: string | null; 
/**
 * 思考可见时长（毫秒）。
 */
thinkMs: number | null; createdAt: number; 
/**
 * 终态落库中断标记（ADR-001）。
 */
interrupted: boolean }
/**
 * 应用配置（FR-009 / ADR-012；wire 形态 camelCase，落盘文件仍为 infra 的 snake_case 键）。
 */
export type ConfigDto = { providers: ProviderDto[]; 
/**
 * 全局默认模型指向的 provider id；null = 未选择。
 */
activeProviderId: string | null; 
/**
 * 打字节奏 ms/字（10–160，FR-009）。
 */
rhythmMsPerChar: number; punctPauseEnabled: boolean; 
/**
 * 动效时长基准 ms。
 */
animDurationBase: number; uiLanguage: string; 
/**
 * system / light / dark。
 */
uiTheme: string; 
/**
 * 导演专用模型；null / 空 = 跟随主模型（INT-003）。
 */
directorModel: string | null }
/**
 * 命令错误的统一 wire 形态。`kind` 是判别字段（camelCase），前端可 switch 分型。
 */
export type IpcError = 
/**
 * 目标不存在或已软删除（ADR-009：对调用方等价）。
 */
{ kind: "notFound"; entity: string; id: number } | 
/**
 * 约束冲突 / 非法入参。
 */
{ kind: "conflict"; message: string } | 
/**
 * 存储后端故障。
 */
{ kind: "storage"; message: string } | 
/**
 * 配置装载 / 校验 / 保存失败（ADR-012：坏文件快速失败，不静默重置）。
 */
{ kind: "config"; message: string } | 
/**
 * 能力尚未接线（生成闭环由 TASK-006 接入）。
 */
{ kind: "unavailable"; message: string }
/**
 * 消息角色（data_model：role CHECK IN ('user', 'assistant')）。
 */
export type MessageRole = "user" | "assistant"
/**
 * 单套 LLM Provider（FR-009；OpenAI 兼容）。
 */
export type ProviderDto = { id: string; name: string; baseUrl: string; apiKey: string; model: string }
/**
 * 会话摘要（FR-007：列表按 updated_at 倒序）。
 */
export type SessionSummary = { id: number; characterId: number; title: string; updatedAt: number }
/**
 * 流式事件（INT-001 v1 四态）。Rust 侧由 [`TauriEventSink`] 发射，
 * TS 侧类型经 tauri-specta 同源生成于 `src/api/generated/bindings.ts`。
 */
export type StreamEvent = 
/**
 * 正文增量（markdown-lite 原文片段）。`reset = true`：重发尝试首事件，清空该
 * message_id 已累积内容后重新累积（「新内容替换旧半条」）。
 */
{ type: "token"; session_id: number; message_id: number; text: string; reset: boolean } | 
/**
 * 思考增量（字段型直通 + 内联 `<think>` 拆分后，FR-003）。`reset` 语义同 Token。
 */
{ type: "reasoning"; session_id: number; message_id: number; text: string; reset: boolean } | 
/**
 * 终态：正常完成，整条已落库（落库由生成编排负责，ADR-001）。
 */
{ type: "done"; session_id: number; message_id: number; think_ms: number | null } | 
/**
 * 终态：失败，半条已落库（中断标记，ADR-001）。
 */
{ type: "error"; session_id: number; message_id: number; reason: string; interrupted: boolean }

/** tauri-specta globals **/

import {
	invoke as TAURI_INVOKE,
	Channel as TAURI_CHANNEL,
} from "@tauri-apps/api/core";
import * as TAURI_API_EVENT from "@tauri-apps/api/event";
import { type WebviewWindow as __WebviewWindow__ } from "@tauri-apps/api/webviewWindow";

type __EventObj__<T> = {
	listen: (
		cb: TAURI_API_EVENT.EventCallback<T>,
	) => ReturnType<typeof TAURI_API_EVENT.listen<T>>;
	once: (
		cb: TAURI_API_EVENT.EventCallback<T>,
	) => ReturnType<typeof TAURI_API_EVENT.once<T>>;
	emit: null extends T
		? (payload?: T) => ReturnType<typeof TAURI_API_EVENT.emit>
		: (payload: T) => ReturnType<typeof TAURI_API_EVENT.emit>;
};

export type Result<T, E> =
	| { status: "ok"; data: T }
	| { status: "error"; error: E };

function __makeEvents__<T extends Record<string, any>>(
	mappings: Record<keyof T, string>,
) {
	return new Proxy(
		{} as unknown as {
			[K in keyof T]: __EventObj__<T[K]> & {
				(handle: __WebviewWindow__): __EventObj__<T[K]>;
			};
		},
		{
			get: (_, event) => {
				const name = mappings[event as keyof T];

				return new Proxy((() => {}) as any, {
					apply: (_, __, [window]: [__WebviewWindow__]) => ({
						listen: (arg: any) => window.listen(name, arg),
						once: (arg: any) => window.once(name, arg),
						emit: (arg: any) => window.emit(name, arg),
					}),
					get: (_, command: keyof __EventObj__<any>) => {
						switch (command) {
							case "listen":
								return (arg: any) => TAURI_API_EVENT.listen(name, arg);
							case "once":
								return (arg: any) => TAURI_API_EVENT.once(name, arg);
							case "emit":
								return (arg: any) => TAURI_API_EVENT.emit(name, arg);
						}
					},
				});
			},
		},
	);
}
