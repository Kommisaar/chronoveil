# 自主迭代记录

> 2026-09-10 夜间自主迭代会话的 Task 日志（coordinator：ZCode）。编号 Task-xx 仅为本
> 日志条目编号，与 relay-harbor 设计条目（FR-xxx / ADR-xxx / TASK-xxx）无关；条目内容
> 若触及设计编号会显式标注。每个 Task 在独立 worktree（`.worktrees/<名>` + 分支
> `iter/<名>`）迭代，由 coordinator 审阅后 rebase 合并回 master，合并前过全量门禁
> （`pnpm run check` + `cargo test`）。只读审计类 Task 不占 worktree，直接读主树，
> 产出为报告。

## Task 总览

| 编号 | 主题 | worktree / 分支 | 状态 |
| --- | --- | --- | --- |
| Task-01 | 聊天流链路测试补强（streamHub / StreamingMessage） | `wave1-chat-tests` / `iter/wave1-chat-tests` | ✅ 已合并 |
| Task-02 | 设置与角色编辑器测试补强（表单逻辑 / 纯函数 / 组件冒烟） | `wave1-features-tests` / `iter/wave1-features-tests` | ✅ 已合并 |
| Task-03 | Rust 侧审计加固（clippy 清零 / 测试补强 / 边界复核） | `wave1-rust-hardening` / `iter/wave1-rust-hardening` | ✅ 已合并 |
| Task-04 | 角色卡导入/导出（JSON 单卡 + 原生对话框） | `wave2-character-io` / `iter/wave2-character-io` | ✅ 已合并 |
| Task-05 | 导演/结算服务实现设计备忘录（只读） | —（主树） | ✅ 备忘录产出，存 [设计备忘-导演结算.md](./设计备忘-导演结算.md) |
| Task-06 | 渲染引擎补强（anims 直测 / 风格表一致性） | `wave1-engine` / `iter/wave1-engine` | ✅ 已合并 |
| Task-07 | mock 后端契约对齐测试 + i18n 奇偶守卫 | `wave1-api-mock` / `iter/wave1-api-mock` | ✅ 已合并 |
| Task-08 | 仓库卫生审计（死代码 / 抑制标记 / 登记一致性，只读） | —（主树） | ✅ 报告产出，存 [审计-仓库卫生.md](./审计-仓库卫生.md) |
| Task-09 | 引擎与 demo 对齐审计（streaming-animations.html vs src/engine，只读） | —（主树） | ✅ 报告产出，存 [审计-引擎demo对齐.md](./审计-引擎demo对齐.md) |
| Task-10 | 修复 render_style 跨端默认值不一致（发现于 Task-02） | coordinator 自修（master 直改） | ✅ 已合并 |
| Task-11 | 导演/结算切片实现（FR-011 结算链路，按 Task-05 备忘录步骤 1–5，零 IPC/前端改动形态） | `wave2-director` / `iter/wave2-director` | ✅ 已合并 |
| Task-12 | 引擎接线修复批次（历史消息 static 渲染 / pre-wrap / 主题色 / 中途调参接线，源于 Task-09 发现 1/2/3/8） | `wave2-engine-wiring` / `iter/wave2-engine-wiring` | ✅ 已合并 |
| Task-13 | 卫生批次 B：死代码清理 / ApiError 消环 / 节奏常量单一来源 / 陈旧 allow | `wave3-hygiene-b` / `iter/wave3-hygiene-b` | ✅ 已合并 |
| Task-14 | 卫生批次 C：测试补盲（relativeTime / i18n index / 动效组件 / 编辑器交互流） | `wave3-test-blindspots` / `iter/wave3-test-blindspots` | ✅ 已合并 |
| Task-15 | 引擎遗留语义修复（Task-09 发现 4/5/7：`- - -` 误判 / 拉丁词拆断 / done-in-think 收尾）+ animBase 校验域对齐 | `wave3-engine-semantics` / `iter/wave3-engine-semantics` | ✅ 已合并 |
| Task-16 | FR-014 开局向导设计备忘录（只读，与 Task-05 备忘录衔接） | —（主树） | ✅ 备忘录产出，存 [设计备忘-开局向导.md](./设计备忘-开局向导.md) |
| Task-17 | Rust 约定微调批次（storage allow 收窄 / is_active / 枚举 doc / ConfigStore cfg_attr） | `wave3-rust-conventions` / `iter/wave3-rust-conventions` | ✅ 已合并 |
| Task-18 | FR-014 开局向导实现切片（四预设历法 + 起始锚 + 首场景 seed + create_session 扩展，按 Task-16 备忘录） | `wave3-fr014-opening` / `iter/wave3-fr014-opening` | ✅ 已合并 |

## 合并账（master 线性历史）

| commit | 内容 | 门禁 |
| --- | --- | --- |
| `5c7801b` | docs: 迭代记录开工 | — |
| `813c267` + `d63adda` | Task-03：Rust clippy 5→0（测试代码等价改写）、新增 14 个边界测试（SSE 冒号/多行/[DONE]/大小写、think 逐字符与误判恢复、注册表槽位/标题截断），120 测试全绿 | 合并后主树复验通过 |
| `f93f9a1` + `4c05953` | Task-02：新增 5 测试文件 63 用例（useEditorForm 20 / posterGradient 14 / pieces 冒烟 14 / SettingsCard 7 / preferences 边界 8），合并后主树 211 用例全绿 | `pnpm run check` + `cargo test` 均过（23:38） |
| `e05e47c` + `b23e335` | Task-01：新增 2 测试文件 27 用例（streamHub 状态机 15 / StreamingMessage 渲染分支 12），合并后主树 238 用例全绿 | `pnpm run check` 过（23:41） |
| `0e2b4f1` + `5033060` | Task-06：引擎 +20 用例（anims 一致性固化 4 + Renderer 回合控制 10 + 摘除/钳制等）、18 风格三方（demo/ANIM_STYLES/engine.css）核对全对齐，源码逻辑零改动（新增 node-env.d.ts 测试类型辅助） | `pnpm run check` 过（23:59） |
| `3b42f55` | Task-07：mock 契约对齐测试 25 + i18n 奇偶守卫 3；修复 5 处 mock 漂移（标题回填/regenerate 返回值契约颠倒/NotFound 语义/排序决胜/ApiError 形态）；en.ts 补 17 缺 key | `pnpm run check` 过（00:02） |

> 备注（Task-07 遗留小疵）：`mock/backend.ts` 引入 `ApiError` 形成与 `commands.ts` 的
> 惰性双向 import（运行时无顶层求值依赖，门禁通过）；后续批次 B 可把 `ApiError`
> 下沉到中性模块消环。

| `c43701f`+`4ac2daa`+`b089d53`+`d6aa59a` | Task-04：角色卡导入/导出纵向切片——IPC `export_character`/`import_character`（tauri-plugin-dialog Rust 侧 API，capabilities 未动，blocking 对话框走 async 命令避主线程互锁）、卡文件纯函数 7 组单测、白名单/bindings/双语/前端 UI/mock 全套 | 主树 `pnpm run check` + `cargo test` + clippy 全过（00:10） |
| `07a788f` | Task-10：迁移 0006 存量 'typewriter'→'type'（'typewriter' 从未在 18 风格表内）、Rust/前端新建默认值同步、mock 种子订正、专项迁移测试 | `cargo test` 131 全绿 + `pnpm run check` 过（00:18） |
| `c119f0a`+`f937e2b`+`d1f0f02`+`88e52b2` | Task-12：四项接线修复——历史 assistant 行接 `renderStaticMarkdown`（收尾不再回退字面星号）、流式容器 pre-wrap、engine.css 主题色收敛 `--cv-*` 变量（暗色默认零回归，聊天容器按 Fluent token 覆写）、tuning 中途热更接 setStyle/setRhythm/setDuration（标量依赖 + isAnimStyleId 守卫） | 主树 `pnpm run check` 过（00:35） |
| `38ea66c`+`3d16f35`+`7108901` | Task-11：FR-011 结算链路落地（+1928 行）——fiction_time/state_expiry 域填充、commit_settlement 单事务端口、director 结算编排（complete_json→normalize→commit，500ms×2 封顶 8s 可打断无硬上限）、生成闭环接线（persist 后 release 前触发，done 放行时账已在库）；+34 测试，集成测试抓出并修复初版漏触发门控 bug | `cargo test` 165+1 全绿 + clippy 0 + check 过（00:51）；rebase 时 ipc.rs import 行与 Task-04 冲突已解 |
| `572c848`..`a0cb382`（6 笔） | Task-13：RenderStyle 死类型删除、ApiError 下沉 errors.ts 消环、节奏常量从引擎再导出、commands.ts 注释更新、models.rs 陈旧 allow 删除；**顺带修复 master 基线上 MockError 未定义的 tsc 构建失败**（Task-04 rebase 语义冲突的漏网，`check` 门禁不含 build 故此前未察觉） | 主树 check + **build** + cargo 165 全过（01:00）；自本笔起合并门禁加入 `pnpm run build` |
| `68b2181`..`6c91597`（5 笔） | Task-17：StoragePort 整块 allow 收窄至 9 个预留方法（逐个注明阶段）；is_active 保留 + 预留注释；LlmError/AppStateError 补枚举 doc；ConfigStore::new/path 按真实使用面收窄、effective_director_model 与 llm.rs 模块 allow 随导演接线转正 | cargo 165 全绿 + clippy 0 + check 过（01:18）；rebase 时 models.rs 注释行与 Task-13 冲突已解（取其更准确版本） |
| `d3adc8d`+`11c3e63`+`6f7bf46`+`1982b45` | Task-15：`- - -` 延迟判定（待决 hold 由下一非空白字符裁决，半包任意切分正确）、takeUnit 拉丁词不拆断（词整块可超粒度）、done-in-think 真实修复（finish 等思考通道收拢再收尾，原 ：231 注释从此为真；streamHub/组件时序依赖已核查）、animBase 校验域对齐 150–1200（i18n 提示同步） | 主树 check（81s 全绿，此前一次失败为并发编译负载抖动）+ build 过（01:23） |
| `a4bf2f2`..`f5e3304`（6 笔） | Task-14：+34 用例补盲——relativeTime 全域（fake timers 钉 Date.now）、i18n index 纯函数、moveIndicator 缓动路径（jsdom WAAPI 桩）、useResolvedTheme（matchMedia stub）、CharacterEditorDialog 主交互流（经真实父级挂载，放弃确认流两分支）；顺带消除一处 Fluent modalizer 竞态 flake（确认框查询改文本定位）；删除按钮行内样式收编 griffel | 主树 check + build 过（01:30） |
| `fcac247`+`1f69ace`+`7f6aaf5`+`cadf5ce` | Task-18：FR-014 开局包/开局向导纵向切片（+1444 行）——四预设历法常量 + validate、create_session 单事务落开局包（显式日历优先/快照兜底 + 开场锚行**无条件** seed，降级路径 day=1/part=夜）、create_session 三参化 + SessionOpeningInput/CalendarConfigDto + CharacterSummary.calendarConfig 透传、侧栏两段式 NewSessionDialog（跟随角色卡/四预设/锚/首场景，三钮含降级）、mock 同款校验 + 种子角色挂旧都历、双语 wizard 组 key + chat.empty 修正 | `cargo test` 173 全绿 + clippy 0 + check + build 过（01:54）；Task-14/18 跨分支语义冲突（夹具缺 calendarConfig 必填字段）由 coordinator 修复合并（`8cdffd0`） |

## 下一批规划（2026-09-11 晚间，待用户开工指令）

> 07:00 硬期限已过，本轮未派发（02:00 提前收摊的流程教训：距期限仍有数小时时不应
> 自设「最后一发」，应滚动规划直至期限）。以下为已备好的批次，届时按此直接开工。

| 候补 Task | 内容 | 预估 | 备注 |
| --- | --- | --- | --- |
| Task-19 | **FR-012 状态面板 + 场景史（一期）**：新增 `list_character_states` / `set_character_state` / `clear_character_state` / `list_scenes` 四个读改命令（whitelist +4 登记、bindings 再生成），ChatView 内可折叠「叙事面板」列状态（scope 分组）与场景史（date_label/location/summary）；消费 Task-17 收窄 allow 时预留的 storage 方法，用后摘豁免；mock + i18n + 组件测试全套 | 大件 1 agent | director.rs 结算语义与 prompt 装配不动 |
| Task-20 | **FR-014 二期「AI 起草历法」**：新命令 `draft_calendar_config(theme)`（复用 resolve_director_llm + complete_json，validate 把关），NewSessionDialog 历法区加「让 AI 起草」入口（成功即选中、失败就地报错）；mock 返回罐头历法 | 中件 1 agent | 与 19 在 ipc.rs/whitelist/bindings 相交，合并时 sequenced + master 重跑 cargo test 收敛 |
| Task-21 | **亮色主题对比度精修**：`.action` #93a5c8（≈2.2:1）/decode #e8b06a/bold 米白在亮色下的变量值重调（双主题可读），personaMarkdown 覆写同步 | 小件 1 agent | 依赖 19 对 ChatView 的改动落地后 rebase |

派发纪律不变：每次派发前查 date；晚间批次上限以「合并 + 收尾能在就寝前完成」为度。

## 收尾总结（2026-09-11 02:00）

一夜自主迭代收官：**18 个 Task**，14 个落地合并、4 个只读产出（Task-05/08/09/16 的
设计备忘与审计报告存 docs/）。master 自 `7b23a92` 起推进约 **40 个提交**。

- **功能**：FR-011 导演/结算链路上线（assistant 落库后线性阻塞结算，场景/状态/日历
  三账齐建）；FR-014 开局向导上线（四预设历法 + 起始锚 + 首场景 seed，每个新会话
  从第一拍就有账可记）；角色卡 JSON 导入/导出（原生对话框）。
- **质量**：前端 vitest 0 → **332 用例**（engine 契约一致性守卫、streamHub/渲染分支、
  mock 契约对齐、i18n 奇偶守卫、迁移专项、补盲全覆盖）；Rust 106 → **173+1 测试**，
  clippy 0 警告；markdown-lite 语法扩展（扁平列表）带全链路测试。
- **修复**：render_style 跨端默认值不一致（迁移 0006）、历史消息不走引擎渲染（样式
  回退字面星号）、`- - -` 列表误判、拉丁词拆断、done-in-think 胶囊瞬时拆除、mock 与
  Rust 的 5 处契约漂移、en.ts 17 缺 key、MockError 构建断裂、engine.css 主题硬编码、
  tuning 中途不生效、animBase 校验域错位、Fluent modalizer 测试 flake。
- **文档**：`docs/迭代记录.md`（本文件）、`设计备忘-导演结算.md`、`设计备忘-开局向导.md`、
  `审计-引擎demo对齐.md`、`审计-仓库卫生.md`。概念文档 §7.6 festivals 键格式的修订
  意见记录于开局向导备忘 §2.1/§7-1，未直改原始资料。
- **遗留与建议**（下夜可选）：场景元信息 UI（list_scenes 读命令，导演备忘二期）、
  FR-012 状态面板/手动锁、FR-014 二期「AI 起草历法」、角色卡日历编辑切片、
  eprintln→log 插件（待用户拍板）、action/decode 亮色对比度精修。
- **流程备注**：rebase 冲突 4 处均小（import 行/注释行/同位加 key），跨分支语义冲突
  1 处（测试夹具）；`check` 不含 tsc build 的盲区已用「合并门禁加跑 build」堵上；
  autocrlf 幻影 M 的处置（`git diff` 核零内容后 restore）已成例行。

## Task-01 聊天流链路测试补强 ✅

- **结果**：27 用例（streamHub 15 + StreamingMessage 12）。streamHub 侧以
  `vi.resetModules()` + 动态 import 做单例隔离，覆盖 begin/markStopping/终态/end 迟到
  事件/多会话隔离/订阅面全迁移路径；StreamingMessage 侧覆盖积压回放、思考先行时序、
  reset 重来、error/cancel/stopping 冻结收尾、切走不取消（卸载后 hub 独立收束）。
  事件注入统一 vi.mock `subscribeStream` 捕获 handler，封闭不依赖 Tauri。
- **源码问题**：无。

## Task-02 设置与角色编辑器测试补强 ✅

- **结果**：63 用例（明细见合并账）；既有用例零改动。
- **发现的问题（未修，升级为 Task-10）**：Rust 侧新建角色默认 `render_style: "typewriter"`
  （`src-tauri/src/domain/models.rs` Default、`infra/storage/characters.rs:170`、
  `interfaces/ipc.rs:1006`），但引擎 18 风格表（`src/engine/anims/index.ts`）中打字机
  的 id 是 `type`——`typewriter` 不存在，导致新建角色预览演出静默回落 fade。跨端数据
  约定问题，修复需协调 Rust 默认值与既有数据，待派发。

## Task-03 Rust 侧审计加固 ✅

- **结果**：clippy（含 rustc）5 → 0；新增 14 测试（明细见合并账）；行为级改动零；
  bindings.ts 零 diff；`check-rust-boundaries` 通过。storage 各仓软删/排序语义复核
  结论：已覆盖充分，未硬凑用例。

## Task-04 角色卡导入/导出 ✅

- **结果**：JSON 单卡导出/导入全链路。文件格式
  `{format:"chronoveil-character",version:1,character:<CharacterInput 同形>}`；对话框用
  tauri-plugin-dialog **Rust 侧 API**（不经过 ACL，capabilities 保持 `core:default`；
  blocking 对话框标 `#[tauri::command(async)]` 避主线程互锁）；分层遵守：对话框/文件
  IO 在 interfaces/ipc.rs，卡 JSON 构建/校验纯函数在 services/character_io.rs（7 组
  单测 + 导出→导入往返保真测试）；前端工具栏导入入口 + 卡片菜单导出，现有组件签名
  零改动；mock 后端浏览器 dev 实现；zh/en 各加 5 key；白名单与 bindings 双登记。
- **取消/失败语义**：对话框取消（None）完全静默；真实错误走视图级就地红字。

## Task-05 导演/结算实现设计备忘录 ✅

- **产出**：全文存 [设计备忘-导演结算.md](./设计备忘-导演结算.md)。要点：结算挂点在
  `generate_once` Completed 分支 persist 之后、`sink.release()` 之前（done 事件被终态
  闸门扣住，天然满足 ADR-005 线性阻塞）；复用 `complete_json` + `effective_director_model`；
  唯一端口扩展 `commit_settlement`（单事务）；触发判定与引擎 `SCENE_LINE_RE` 逐行对齐；
  第一期只落库零前端改动；6 步拆解每步可独立验证。8 个开放问题均附推荐项（§7）。
- **后续**：Task-11 按备忘录步骤 1–5 实现，开放问题按 §7 推荐项执行。

## Task-06 渲染引擎补强 ✅

- **结果**：+20 用例（合并后主树 258）；18 风格一致性（id/label/data-anim 选择器/
  boxed⇔inline-block/durFactor↔CSS 倍率）逐项核对全对齐，并固化 4 个一致性测试——
  元数据与 engine.css 后续漂移会直接挂测试。引擎源码逻辑零修改。

## Task-08 仓库卫生审计 ✅

- **产出**：全文存 [审计-仓库卫生.md](./审计-仓库卫生.md)。总体卫生很好：无 TODO/
  调试遗留/TS 抑制标记，IPC 与依赖登记三方一致。要害发现：**en.ts 缺 17 个 zh key**
  （英文界面回落中文）→ Task-07 飞行中覆盖；节奏常量双份定义（漂移风险）、若干死
  导出/陈旧 allow → 修复批次 B/C/D 待派发（含 relativeTime、CharacterEditorDialog
  测试盲区）。待人工确认项单列，不自动处置。

## Task-07 mock 后端契约对齐 + i18n 守卫 ✅

- **结果**：+28 用例；修复 5 处 mock 与 Rust 的真实漂移（含 regenerateLast 返回值契约
  颠倒——Rust 返回被替换旧条而 mock 返回新条；标题回填缺失；NotFound 语义；排序
  决胜；错误 wire 形态），逐条附 Rust 侧行号依据；**en.ts 17 个缺失 key 全部补齐**
  （Task-08 批次 A 就此关闭），并落了 zh/en 深层 key 奇偶 + 占位符一致的守卫测试。

## Task-08 仓库卫生审计（进行中，只读）

- **方向**：死代码/未用导出、遗留调试、抑制标记（@ts-ignore / unwrap）、TODO 清单、
  i18n 未用 key、IPC/依赖登记三方一致性、测试盲区；产出按可派发修复批次组织。

## Task-09 引擎与 demo 对齐审计 ✅

- **产出**：全文存 [审计-引擎demo对齐.md](./审计-引擎demo对齐.md)。结论：核心机制
  （信用时钟/追速/微停/封存/思考通道/18 风格/decode/光标）与 demo 逐参数一致；18/18
  风格三方对齐；列表扩展为带完整测试的单向演进。
- **行动项**：高优先级「历史消息不走引擎渲染」（ChatView 直接插纯文本，收尾后样式
  全丢）→ Task-12；中优先级 pre-wrap 缺失、engine.css 暗色硬编码、中途调参未接线 →
  Task-12；低优先级 `- - -` 列表误判、takeUnit 拆断拉丁词、done-在思考中的胶囊瞬时
  拆除 → 待 Task-06 合并后另批处理（避免同文件冲突）。

## Task-13/14/15/16（2026-09-11 00:40 批次，进行中）

- **Task-13 卫生批次 B**：RenderStyle 死类型删除、ApiError 下沉中性模块消环、节奏
  常量从引擎单一来源引入、commands.ts 过时注释、models.rs 陈旧 allow 移除。
  红线：不碰 Task-11 正在工作的 Rust 文件。
- **Task-14 卫生批次 C**：relativeTime / i18n index / indicatorMotion / useResolvedTheme
  纯逻辑与组件测试补盲 + CharacterEditorDialog 交互流测试（放弃确认流为主）+
  marginRight 入 makeStyles 微修。
- **Task-15 引擎遗留语义**：Task-09 发现 5（`- - -` 列表误判，延迟判定修复）/
  7（takeUnit 拉丁词不拆断）/ 4（done-in-think 胶囊收尾时序，倾向真实修复 +
  时序依赖核查）；顺手 preferences animBase 校验域对齐引擎 150–1200。
- **Task-16 FR-014 开局向导设计备忘 ✅**：全文存
  [设计备忘-开局向导.md](./设计备忘-开局向导.md)。要点：仅扩展 `create_session`
  （零新命令、whitelist 零变化）；锚 = scenes idx-0 行（不迁移、不加列）；四预设历法
  内置（**festivals 键以实现为准**——年内日序整数，概念文档的「月名·日号」与代码有
  偏差，推荐文档加修订注）；降级路径也无条件 seed 开场行；一期不做自动拟历/日历
  编辑器/回写角色卡。开放问题 7 项全部有推荐且实现切片按推荐执行。
- **Task-18 FR-014 实现切片**（00:05x 派发）：按备忘录 §6 六步 + §7 推荐项执行，
  预期 bindings 再生成有 diff（新 DTO + CharacterSummary.calendarConfig）。

## Task-11 导演/结算切片实现 ✅

- **结果**：按备忘录步骤 1–5 全量落地（+1928 行），零 IPC 面/零前端改动、零新 crate。
  域层 `fiction_time`（CalendarConfig/parse/date_label/clamp_day/PARTS）与
  `state_expiry`（三义常量/Expiry/parse）从占位转正；唯一端口扩展 `commit_settlement`
  单事务（新场景行 + 上行 summary 回写 + messages.scene_id 归属 + 状态 upsert/软删，
  回滚无痕）；`run_settlement` 退避重试可打断；`GenerationDeps.director_llm: Option`
  未配置则跳过；集成测试断言「含 `---` → done 放行前账已在库」。+34 测试，集成测试
  抓出并修复初版漏触发门控 bug。
- **备忘录留白处的最小裁定**（注释留痕）：expiry 非三义修正为 None 保留条目；
  close_summary 双写上一行使其自洽；AttachRange 起点取触发消息前最近场景线消息；
  §7 开放问题全部按推荐项执行。
- **CRLF 备注**：autocrlf=true 下 cargo test 再生成 bindings.ts 会现 LF 行尾幻影
  M（内容零 diff），`git checkout` 还原即可，非 IPC 变更。

## Task-12 引擎接线修复批次 ✅

- **结果**：四项全落地（对应 Task-09 发现 1/3 排版/2/8）。亮点：engine.css 顶部
  `:root` 定义 6 个 `--cv-*` 引擎命名空间变量（默认 = 原暗色，无覆写零回归），覆写
  单点在聊天流容器（历史行 + 流式行全部生效）；红线约束（Task-06 一致性测试的选择器
  与结构）原样通过，另加 1 条变量定义/引用闭环守卫。新增 ChatView.history.test 4 用例
  + StreamingMessage 热更 4 用例 + 排版冒烟。
- **遗留**：action/decode/bold 的亮色对比度仅变量化 + 注释评估（#93a5c8≈2.2:1 等，
  低于 AA），视觉未动——后续主题精修另行处理。

## Task-10 render_style 跨端默认值修复 ✅（coordinator 自修）

- **结果**：迁移 0006 把存量行 'typewriter' 订正为引擎真实 id 'type'（0001 起的默认串
  从未在 18 风格表内，渲染端一直静默回落 fade）；`NewCharacter::default` 与前端
  `useEditorForm` 新建默认同步改 'type'；mock 种子角色订正；编辑器表单测试按新默认
  重写，「表外串回落 fade」兜底行为改用显式 legacy 串继续覆盖；新增 0006 专项迁移
  测试（遗留串订正、其他风格不动）。Rust 131 测试 + 前端全量 check 绿。
- **注**：0001 列默认值随历史迁移保留不改（插入恒显式传值，已是死 artifact）。

## 窗口批次：叙事账本 + 亮色达标 + AI 起草历法（2026-09-12 05:45）

凌晨窗口（约 01:20–05:45）自主迭代收官：**Task-08~20 共 13 个任务**，12 个落地合并、
1 个纯验证产出（Task-20 真实回路回归）。master 自 `234b879` 推进至 `7b89f21`
（14 个合并提交），全部任务走 verifier（+reviewer 或合并轮审查）双门禁。

- **叙事账本（状态面板/场景史）**：`list_scenes` / `list_character_states` 读命令
  （camelCase DTO、墓碑过滤、NotFound 同构）+ ChatView 右侧「叙事账本」面板
  （状态两组/场景倒序/时间标签链/recap 折叠，终态经 streamHub.onTerminal 自动刷新，
  ADR-005 时序对齐）。用户从「看不见账」到一键开账本。
- **亮色主题达标**：引擎自适应主题注入（src/engine/theme.ts，零依赖纯 TS+DOM，
  祖先链表面探测），action 2.48→5.82、bold 1.22→9.65、decode 1.94→5.79（白底全部
  ≥4.5:1），暗色 `:root` 契约锚定零回归，WCAG 守卫测试锁死。
- **AI 起草历法全链闭环**（FR-014 二期）：`draft_calendar` 命令（schema prompt +
  snake/camel 双键容错 + 钳制 + 越年节日兜底 + 一次修正重试，复用 complete_json 零
  infra 扩展）→ 编辑器历法区块（查看/编辑/四预设下沉 components + AI 起草对话框）
  → `update_character` 接通 `calendarConfig` 持久化（validate → snake_case 存储，
  None=清除）→ 建会话快照 → 结算/装配消费。真实冒烟 2.82s 出「四季十二辰历」。
- **质量与裁决**：C1 `Message.scene_id` 开进读路径——装配/编年史/探索器从位置推断
  改 id 精确对应（欠账偏移不再累积，重生成收敛空段）；C2 移除结算预填（边界快照
  只属于被收束的场景，陈旧 recap/summary 错位根除，`UpdateCharacter` COALESCE 保留）；
  C3 前端加固（ActivityStep.at 死字段移除、drive() never 穷尽断言前移编译期）；
  hr.scene 偶发抖动根因修复（200 轮探针实锤：findByText 与被动 effect 时序差，
  waitFor 收口）；C4/C5/C6/C7 候选全清零。
- **真实回路回归（Task-20，openrouter/free）**：六链全过——对话生成（3 轮真实流式）、
  结算（新行恒 None 实证 Task-09、盖章区间与 AttachRange 精确吻合、欠账自愈）、装配
  五段（含桥场加厚与状态段）、记忆探索（事件序列+命中「铜钥匙」事实）、AI 起草
  （四季十二辰历 validate 过）、历法持久化（读回逐字节一致+快照同证）。免费档日配额
  50 次耗尽为唯一未竟（reset 2026-09-12T00:00Z）。
- **规模**：Rust 229 → 261+1 测试；前端 369 → 441 用例（+tsc 门禁两处补跑逮住 1 个
  漏网类型错误——`check` 不含 tsc 的盲区再次被合并轮审查堵住）。
- **遗留候选**（下窗口可选）：`daysPerMonth` 前端无上界与 AI 路径钳制不对称（存量）；
  HistoryMessageBody 改 useLayoutEffect 消历史行首帧空白（生产行为变更，待拍板）；
  账本面板 present（在场角色）与 timeNote 未展示（有明确挂点）；账本面板开合动效；
  search_history 混合态临时标签与 read_scene 的极端错位（注释已文档化）。
- **流程备注**：并发提升至 6 后单夜 13 任务零冲突合并（冲突域预判 + 契约冻结先行
  开发的并行模式两度奏效：Task-13 按 Task-12 冻结契约先写、合并零偏差）；tsc 补跑
  正式纳入前端任务门禁；bindings 幻影处置已成例行。

## 增补批次：三项拍板落地（2026-09-12 08:15）

用户批准的遗留三项全部合并（Task-21/22/23，master 推进至 `f82e112`）：

- **useLayoutEffect**（`b1898a3`）：HistoryMessageBody 引擎直插前移到绘制前，
  消历史行首帧空白；同款候选排查出两处（StreamingMessage:98 有 onSettled 时序
  连带、pieces.tsx:451 同款）留后续裁决。
- **daysPerMonth 上界**（`0c6c393`）：前端 999 含端点严格对齐 calendar_draft；
  MAX 常量导出、ERROR_KEYS 穷举闭环、输入 max 属性、i18n 同步；历史脏数据走
  「展示-保存即修复」语义无困死。
- **log 插件化**（`cbad39b`+`f82e112`）：log facade + tauri-plugin-log 组合根
  装配（Stdout/Info，留 LogDir 扩展位）；12 处 eprintln 按级别替换（9 warn 3
  error），domain 引 facade 的许可理由已注；eprintln 归零、cargo 零警告。

规模：Rust 261+1 / 前端 442 / tsc 全绿。新候选 C8（generation.rs:398 注释级别
措辞一词修正；check-rust-boundaries 对 use tauri_plugin_log 不设防的守卫盲区）。

## balance 批次：四任务全清（2026-09-12 09:05）

balance 窗口（08:20–09:05，派发截止 09:00 前最后一个 builder 08:32 派出）：
Task-24~27 四任务全部合并，master 推进至 `0e71a0e`。

- **C8 卫生批**（`bd57a3e`）：generation.rs:398 注释级别对齐（error→warn）；
  守卫脚本正则改下划线捕获——`tauri_plugin_log`/`tauri_plugin_dialog` 同族盲区
  补齐（15 用例正则实证 + services 注入端到端验证），`taurix` 不误伤。
- **多角色群像设计备忘**（`51dd92e`）：207 行代码事实盘点（4 意外就绪/4 意外
  阻塞——wire 层角色维度已透传、messages.character_id 是死列、单线程管道才是
  真阻塞）、5 切片泛化草案、Q1–Q6 待拍板。下次立项直接从备忘出发。
- **账本加分项**（`636c2c4`）：场景行补「在场：…」角色名行（id→name 映射、
  未知回退）与 timeNote 叙事时间原文（链首优先、数字兜底）。
- **场景窗口可选**（`3cfed02`+`0e71a0e`）：config.near_scenes（缺键回落 2、
  1–6 越界拒绝同 rhythm 风格）→ generation_deps 派生穿透 → AssembleInputs →
  near_view，穿参链单点无旁路；设置页新「上下文」卡片（防抖自动保存）；
  默认 2 的全部既有行为测试原样全绿。

规模：Rust 261 → 264+1；前端 442 → 448；tsc 全绿。事故与修正：Task-24 合并
曾被 verifier 留下的 bindings 幻影 M 阻塞而落账脚本未中止，产生一次误标
MERGED——已从对象库恢复提交、rebase 真实合并（bd57a3e）并在账本追记修正；
流程教训「worktree rebase 前先清幻影」已注记。新候选 C9（双默认常量绑定 +
config 回落缺 warn）。

## 功能批次：LLM 调用轨迹面板（2026-09-12 13:10）

用户指定功能（参考外部行式+展开式轨迹查看器）：当前会话每次 LLM 调用可查。
Task-28/29 冻结契约双线并行，master 推进至 `c14e308`。

- **后端**（`b36912a`+`dcc501e`+`0872b77`）：迁移 0008 `llm_calls` 表（完整
  prompt_json/response/reasoning/tool_calls/usage/耗时/状态，不做软删）；
  `LlmClient` 单点记录——`CallTrace{session_id,kind}` 尾参穿参恰 4 处全接
  （对话/探索器每轮/结算含重试/起草），每 HTTP 请求一条（重试各记）；
  `LlmCallSink` trait 组合根注入，`TauriCallSink` 先落库成功才发事件；
  SSE usage 终帧解析（不加 include_usage，兼容保守）；`list_llm_calls` +
  `StreamEvent::Trace` 双通道。
- **前端**（`707cd70`+`c14e308`）：账本面板第三段【调用轨迹】——会话汇总条
  （N 次/失败数/IN·OUT 合计）、倒序条目（kind 徽标/tok/耗时/时间，error 红标）、
  单行展开（请求按 role 分色/回复/思考/工具调用，大 JSON 展开时才 parse）；
  `onTrace` 实时头部插入 + 同 id 幂等；trace 走独立 `subscribeTraces` 通道
  （协调者补全量导出对齐后端 `subscribeTrace` 过滤形态），不进 StreamEvent
  联合、不触 drive 穷尽断言。

规模：Rust 264 → 281+1；前端 448 → 466；tsc 全绿。新增候选 C10：
error_text 无截断、轨迹表无清理归档（重度 10–15MB/天）、attempt 序号列。
**事故备忘**：本轮两 agent（28 verifier/29 builder）完成通知未送达（平台投递
bug），用户人工发现；协调者经 worktree git 态 + 输出文件受控抽取确认结果。
另 Task-24 合并曾被幻影 M 阻塞后误标 MERGED，已恢复并账本追记——「rebase 前
清幻影」升级为合并脚本固定步骤。

## 多角色第 1 步：会话角色实例层（2026-09-12 22:40）

九项决策全拍板（模板/实例分离、扮演位必选、逐拍独立生成、离场照常清算、roster
无上限、动态人物手动收编、greeting 移除、无迁移负担、「对XX」关系键），最终方案
入 docs/方案-多角色与时间线-最终.html。实施三步链的第 1 步完成：

- **Task-30 Rust 全链**（`fd09945`..`1f6e5a1`，四提交）：迁移 0009——
  character_instances 新表（is_user 恰一硬校验、可空溯源）+ sessions 删
  character_id + messages 死列激活为 instance_id + 状态换挂与唯一索引简化 +
  llm_calls 重建保形；建会话阵容制单事务（逐卡实例化快照 + 锚行 present=全实例）；
  装配多实例（用户位段/LLM 位分段/群像语境声明/状态多持有者分节）；结算裁决
  schema 键名 instance_id（破坏性，untagged 拒旧重试）；assistant 归属主持实例
  （首个 LLM 位）；v1.5 简化 6 条注释在案（多 LLM 位单次生成，逐拍属第 2 步后）。
- **Task-31 wire+UI**（`7f595db`..`aaa00a3`，六提交）：stub 契约先行 → Task-30
  合入后 rebase 对齐删 stub；阵容回显 SessionInstanceDto（含 renderStyle 快照，
  消除回查模板卡的孤儿请求）；开局向导两步选人（扮演位门槛 + 阵容多选海报卡，
  同卡双位合法）；speakerOf/账本在场行按实例取名；mock D3 校验对齐存储层
  （快修：反向契约断言纠正 + 幽灵分支收敛）。
- 规模：Rust 281 → 290+1；前端 466 → 473；tsc 0；clippy 零新增。
- verifier 拦截：MAJOR-1（mock 缺 D3 校验且注释反向断言）快修收口；MAJOR-1'
  （0009 在带存量 v8 库升级 FK 失败，全新库口径达标放行）→ 候选 C11（迁移执行器
  FK=OFF 包络）。事故一次：两 agent 完成通知未送达，经 worktree git 态确认。
- 下一步：第 2 步「状态历史化」（append-only + superseded_at，分叉的前置），
  C9/C10/C11 卫生批可并行。
