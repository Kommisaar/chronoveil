# 自主迭代记录

> 2026-09-10 夜间自主迭代会话的 Task 日志（coordinator：ZCode）。编号 Task-xx 仅为本
> 日志条目编号，与 relay-harbor 设计条目（FR-xxx / ADR-xxx / TASK-xxx）无关；条目内容
> 若触及设计编号会显式标注。每个 Task 在独立 worktree（`.worktrees/<名>` + 分支
> `iter/<名>`）迭代，由 coordinator 审阅后 rebase 合并回 master，合并前过全量门禁
> （`pnpm run check` + `cargo test`）。只读审计类 Task 不占 worktree，直接读主树，
> 产出为报告。

## Task 总览

| 编号 | 主题 | worktree / 分支 | 状态 |
| --- | --- | --- | --- |
| Task-01 | 聊天流链路测试补强（streamHub / StreamingMessage） | `wave1-chat-tests` / `iter/wave1-chat-tests` | 进行中 |
| Task-02 | 设置与角色编辑器测试补强（表单逻辑 / 纯函数 / 组件冒烟） | `wave1-features-tests` / `iter/wave1-features-tests` | ✅ 已合并 |
| Task-03 | Rust 侧审计加固（clippy 清零 / 测试补强 / 边界复核） | `wave1-rust-hardening` / `iter/wave1-rust-hardening` | ✅ 已合并 |
| Task-04 | 角色卡导入/导出（JSON 单卡 + 原生对话框） | `wave2-character-io` / `iter/wave2-character-io` | 进行中 |
| Task-05 | 导演/结算服务实现设计备忘录（只读） | —（主树） | 进行中 |
| Task-06 | 渲染引擎补强（anims 直测 / 风格表一致性） | `wave1-engine` / `iter/wave1-engine` | 进行中 |
| Task-07 | mock 后端契约对齐测试 + i18n 奇偶守卫 | `wave1-api-mock` / `iter/wave1-api-mock` | 进行中 |
| Task-08 | 仓库卫生审计（死代码 / 抑制标记 / 登记一致性，只读） | —（主树） | 进行中 |
| Task-09 | 引擎与 demo 对齐审计（streaming-animations.html vs src/engine，只读） | —（主树） | 进行中 |
| Task-10 | 修复 render_style 跨端默认值不一致（发现于 Task-02，待派发） | 待开 | 未开始 |

## 合并账（master 线性历史）

| commit | 内容 | 门禁 |
| --- | --- | --- |
| `5c7801b` | docs: 迭代记录开工 | — |
| `813c267` + `d63adda` | Task-03：Rust clippy 5→0（测试代码等价改写）、新增 14 个边界测试（SSE 冒号/多行/[DONE]/大小写、think 逐字符与误判恢复、注册表槽位/标题截断），120 测试全绿 | 合并后主树复验通过 |
| `f93f9a1` + `4c05953` | Task-02：新增 5 测试文件 63 用例（useEditorForm 20 / posterGradient 14 / pieces 冒烟 14 / SettingsCard 7 / preferences 边界 8），合并后主树 211 用例全绿 | `pnpm run check` + `cargo test` 均过（23:38） |

## Task-01 聊天流链路测试补强（进行中）

- **动机**：`src/features/chat/` 是全应用最核心的链路（多路并发生成、切走不取消、
  终态落库），但 `streamHub.ts`、`StreamingMessage.tsx` 无单测。
- **范围**：仅新增 `src/features/chat/*.test.ts(x)`；发现源码 bug 只记录 + 最小修复。

## Task-02 设置与角色编辑器测试补强 ✅

- **结果**：63 用例（明细见合并账）；既有用例零改动。
- **发现的问题（未修，升级为 Task-10）**：Rust 侧新建角色默认 `render_style: "typewriter"`
  （`src-tauri/src/domain/models.rs` Default、`infra/storage/characters.rs:170`、
  `interfaces/ipc.rs:1006`），但引擎 18 风格表（`src/engine/anims/index.ts`）中打字机
  的 id 是 `type`——`typewriter` 不存在，导致新建角色预览演出静默回落 fade。跨端数据
  约定问题，修复需协调 Rust 默认值与既有数据，待派发。

## Task-03 Rust 侧审计加固 ✅

- **结果**：clippy（含 rustc）5 → 0；新增 14 测试（明细见合并账）；行为级改动零；
  bindings.ts 零 diff；`check-rust-boundaries` 通过。storage 各仓软删/排序语义复核
  结论：已覆盖充分，未硬凑用例。

## Task-04 角色卡导入/导出（进行中）

- **形态**：JSON 单卡导出/导入（`{format:"chronoveil-character",version:1,character:<CharacterInput 同形>}`），
  新增 IPC `export_character(id)` / `import_character()`，文件对话框用
  `tauri-plugin-dialog` Rust 侧 API（不加 npm 依赖）；mock 后端同步补浏览器 dev 实现；
  白名单 + i18n 双语登记。

## Task-05 导演/结算实现设计备忘录（进行中，只读）

- **产出**：结算挂点（ADR-005 线性阻塞落地）、结算 LLM 调用 JSON schema、ports 扩展、
  日历接线、前端可见性最小集、分阶段拆解、需用户拍板的开放问题。作为后续实现任务的输入。

## Task-06 渲染引擎补强（进行中）

- **方向**：anims 目录直测（ANIM_STYLES 元数据 ↔ engine.css 18 风格一致性核对）、
  Renderer 未覆盖行为（中途 setStyle、replayInstant、cancel 语义、onFinish 字数口径、
  兜底开演路径）。

## Task-07 mock 后端契约对齐 + i18n 守卫（进行中）

- **方向**：mock/backend 行为测试对照 Rust ipc.rs 同契约；新增 i18n zh/en key 奇偶
  守卫测试（深层 key 集合一致、值非空），发现缺 key 顺手补齐。

## Task-08 仓库卫生审计（进行中，只读）

- **方向**：死代码/未用导出、遗留调试、抑制标记（@ts-ignore / unwrap）、TODO 清单、
  i18n 未用 key、IPC/依赖登记三方一致性、测试盲区；产出按可派发修复批次组织。

## Task-09 引擎与 demo 对齐审计（进行中，只读）

- **方向**：docs/streaming-animations.html（活参考）vs src/engine 逐机制对齐表
  （信用时钟/追速/微停/封存/思考通道/18 风格仪式/decode/光标），单向演进清单 +
  回归疑点清单。

## Task-10 render_style 跨端默认值修复（待派发）

- **来源**：Task-02 发现（详见 Task-02 节）。
- **方向**：统一「打字机」风格 id（改 Rust 默认值为 `type` 并评估存量行迁移，
  或前端映射别名——倾向前者，需核对引擎风格表 id 权威来源后定案）。
