# ChronoVeil 代码卫生审计报告（Task-08 产出）

> 2026-09-10 夜间自主迭代，只读审计。基线 master @ 5c7801b。总体结论：仓库卫生状况
> 很好（无 TODO/调试遗留/TS 抑制标记，IPC 与依赖登记三方完全一致），主要问题集中在
> **i18n 中英文资源漂移**和**少量死导出/陈旧注释**。

## 1. 死代码与未用导出

**中**

- **节奏常量双份定义（漂移风险）**：`src/engine/index.ts:41-43` 的
  `RHYTHM_MIN_MS/MAX/DEFAULT`（10/160/45）外部零引用，`src/features/settings/preferences.ts:12-13`
  另有一份同值域常量。建议合并为单一来源。

**低**

- `src/api/types.ts:29` `RenderStyle = string` 全仓零引用，可删。
- `posterGradient.ts:72,78` `withAlpha`/`DEFAULT_POSTER_GRADIENT` 仅测试消费，去
  export 或标注测试专用。
- `models.rs:171` `#[allow(dead_code)] NewMessage::new` 已陈旧（`ipc.rs:471` 已调用），allow 可删。
- `infra/storage.rs:82` 整块 allow 覆盖 26 方法（11 个为 FR-011/012 预留、仅测试调用），
  建议收窄到预留方法并注明阶段。
- `generation.rs:137` `GenerationRegistry::is_active` 仅本文件测试调用。
- engine barrel 导出面大于消费面——引擎定位为独立可测库，设计使然，仅提示。

**待人工确认（计划占位，勿直接删）**：`EnsemblePanel.tsx` 空壳（UC-002/FR-010 预留）；
`director.rs`/`fiction_time.rs`/`state_expiry.rs` 一行占位（Task-11 正在填充后两者）；
`config.rs:161` `effective_director_model`（导演切片接线后转正）；`ConfigError::Serialize`
防御变体；`ConfigStore::new/path` 仅测试用。

## 2. 遗留调试

console.*/debugger/println!/dbg!（非测试）：**0 处**。`eprintln!` 3 处均为错误路径
兜底日志且带设计注释（`events.rs:95`、`generation.rs:420,445`）。待确认：Windows GUI
release 无 stderr，日志实际不可见，长期建议引入 `log`/`tauri-plugin-log`。

## 3. 抑制标记

TS 抑制标记（除 generated）：**0 处**。Rust 非测试 unwrap/expect 全部可辩护
（组合根初始化、OnceLock 装配不变式、锁中毒 = 进程级故障、mock 测试基建），无需改动。

## 4. TODO / FIXME / HACK / XXX

全仓 **0 处**。

## 5. i18n

**中（会咬人）**

- **en.ts 缺 17 个 zh.ts 已有 key**，`fallbackLng: 'zh'` 导致英文界面这些位置回落
  显示中文：`chat.empty`、`characters.title/new/empty/cancel/delete/deleteFailed`、
  `settings.title/provider/baseUrl/apiKey/model/deleteConfirmTitle/saving/saveFailed/loadFailed/cancel`。
  → 处置：Task-07（i18n 奇偶守卫，飞行中）会补齐并加守卫测试。

**低**

- zh/en 都有但 UI 未引用：`characters.sectionBasic/sectionStage/sectionLook/back`
  （疑似编辑器分区标题预留，删或接线二选一，zh/en 必须同步动）。
- 中英同值 5 条均为专有名词/语言自指，合理不译。

## 6. 登记一致性

- IPC 四方（whitelist ↔ commands.ts ↔ collect_commands! ↔ bindings）**完全一致**，
  有门禁护航。
- 依赖白名单 ↔ package.json **完全一致**。
- 低：`IpcError::Unavailable` 变体已无构造点（导演切片可能复用），`commands.ts:82`
  注释过时——更新注释或明确废弃。

## 7. 样式 / 约定漂移

内联 style 11 处均为数据驱动动态值，符合惯例；主体均为 griffel，符合 AGENTS.md。
低：`CharacterEditorDialog.tsx:609` 静态 `marginRight: 'auto'` 应入 makeStyles。
待确认：设置页动效基准校验域 0–u32::MAX 与引擎渲染钳制 150–1200ms 不一致（设置 UI
未展示该域）。Rust 公开结构中文注释覆盖良好；仅 `LlmError`（`llm.rs:91`）、
`AppStateError`（`state.rs:38`）两枚举缺一行 `///`。

## 8. 测试盲区

Rust 侧无实质盲区。前端侧：

- 中：`src/lib/relativeTime.ts` 纯函数完全无测（Intl 边界最易补）；
  `CharacterEditorDialog.tsx` 全仓最复杂交互组件（放弃确认流等）无专属测试。
- 低：`i18n/index.ts` 纯函数、`useResolvedTheme`/`useRevealOnScroll`/`indicatorMotion`
  无直接测试。合理无测项（api/client 一行、mock、壳层、ui store 间接覆盖）不列为问题。

## 建议修复批次

| 批次 | 内容 | 状态 |
| --- | --- | --- |
| A | i18n 资源修复（en 补 17 key、4 个未用 key 处置） | **Task-07 飞行中覆盖** |
| B | 死代码与登记卫生（RenderStyle 删除、节奏常量去重、陈旧 allow、Unavailable 注释等） | 待派发 |
| C | 测试补盲（relativeTime / i18n index / CharacterEditorDialog 交互） | 待派发 |
| D | 约定微调（marginRight 入 makeStyles、两枚举补 doc、animBase 域对齐评估） | 待派发 |

待人工确认清单（不要自动处置）：3 处 eprintln 是否引入 log 插件；占位模块去留时点；
animBase 设置域与引擎钳制域不一致是否有意；`ConfigError::Serialize` 防御变体保留与否。
