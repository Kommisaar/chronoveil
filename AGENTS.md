<!-- design-guidelines:start -->
## 设计协作准则

relay-harbor 中状态为“已确认”的设计条目构成当前设计基线；“评审中”条目仅作参考，终态（已取消 / 已替代 / 已废弃）条目不再是有效约束。处理涉及设计的开发任务时遵循：

1. **改码先查设计**：修改需求行为、业务规则、组件边界、接口、数据结构或关键交互前，先经 relay-harbor 查阅相关设计条目（按编号 get_item，或用 get_context 查看影响面）；“评审中”及终态条目不得继续当作有效约束。
2. **适用时携带编号**：存在对应设计编号时，在任务说明以及用户要求创建的 commit 中携带编号，例如 `FR-003`、`UC-002` 或 `CMP-002`；没有对应编号时不臆造。
3. **偏差不得静默实现**：发现实现条件与已确认设计冲突时，先向用户说明冲突证据、受影响编号和建议。获得设计修改授权后，先经 MCP 修订对应条目（编辑已确认条目会使其自动退回“评审中”）；设计重新确认前，不把偏差写成既定结论。若你是执行模式派出的实现或评审子代理，不直接修订设计条目：建 OQ 条目并回报，由主控统一修订。
4. **新需求先进设计**：新的功能需求、业务规则或架构方向先进入 relay-harbor 修订流程，确认并恢复受影响条目的一致性后再实现，不直接以代码替代设计决策。

设计条目的类型、状态与关系约定以 relay-harbor 项目与应用文档为准；设计历史由条目修订记录承载，不另行维护变更日志。
<!-- design-guidelines:end -->
